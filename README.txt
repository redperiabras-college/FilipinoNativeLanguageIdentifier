open and run the app.py to start the flask server then go to your browser and go to localhost:5000

Language: Python 3.5.1
Required Libraries:
	*flask
        *json
        *collections
        *re
        *sys